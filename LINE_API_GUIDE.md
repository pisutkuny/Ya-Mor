# วิธีตั้งค่า LINE Messaging API (สำหรับระบบแจ้งเตือน)

เนื่องจาก LINE Notify ปิดให้บริการ เราต้องเปลี่ยนมาใช้ **LINE Messaging API** แทนครับ ซึ่งจะทำให้บอท "ยาหมอ" ส่งข้อความหาเราได้โดยตรง

## สิ่งที่ต้องเตรียม
1. บัญชี LINE ปกติของคุณ
2. คอมพิวเตอร์ (ทำบนมือถือจะยากหน่อยครับ)

---

## ขั้นตอนที่ 1: สร้าง Provider และ Channel
1. เข้าไปที่เว็บ [LINE Developers Console](https://developers.line.biz/)
2. กดปุ่ม **Log in with LINE account** (ใช้อีเมล/รหัสเดียวกับที่เล่น LINE)
3. ถ้าเข้าครั้งแรก ใส่ชื่อกับอีเมลผู้พัฒนา (มั่วได้) แล้วกด Create Account
4. กดปุ่ม **Create a new provider** (สีเขียว)
   - **Provider name:** ตั้งชื่ออะไรก็ได้ เช่น `Ya-Mor-System`
   - กด **Create**
5. เลือก **Create a Messaging API channel** (ไอคอนรูปข้อความ)
   - **Channel name:** ชื่อบอทที่จะส่งข้อความหาเรา เช่น `Ya-Mor Alert`
   - **Channel description:** ใส่สั้นๆ เช่น `Medical Alert`
   - **Category:** เลือกอะไรก็ได้ (เช่น Health)
   - **Sub-category:** เลือกอะไรก็ได้
   - ติ๊กถูกหน้าฉันยอมรับเงื่อนไขทั้ง 2 ข้อ
   - กด **Create**

---

## ขั้นตอนที่ 2: เอาค่าที่ต้องใช้ (Token & User ID)

### 1. Channel Access Token (กุญแจสำหรับส่งข้อความ)
1. กดที่แท็บ **Messaging API** ด้านบน
2. เลื่อนลงมาล่างสุดจนเจอหัวข้อ **Channel access token**
3. กดปุ่ม **Issue**
4. คุณจะได้รหัสยาวๆ มาหนึ่งชุด -> **ก๊อปปี้เก็บไว้** (นี่คือ `Channel Access Token`)

### 2. User ID (รหัสประจำตัวของคุณ)
1. กดที่แท็บ **Basic settings** ด้านบน
2. เลื่อนลงมาล่างๆ จนเจอหัวข้อ **Your user ID**
3. คุณจะเห็นรหัสยาวๆ (มักขึ้นต้นด้วย U...) -> **ก๊อปปี้เก็บไว้** (นี่คือ `User ID`)

*(หมายเหตุ: ปกติ User ID จะเป็นความลับ แต่ในที่นี้เราใช้ส่งหาตัวเองคนเดียวครับ)*

---

## ขั้นตอนที่ 3: เริ่มใช้งาน
1. เปิดแอป **Ya-Mor** ของคุณ
2. ไปที่หน้า **"ตั้งค่าระบบ" (Settings)**
3. นำค่าที่ได้มาใส่ในช่อง:
   - **Channel Access Token**
   - **Your User ID**
4. กดบันทึก

เสร็จเรียบร้อย! 🎉
ตอนนี้เวลาคุณยายกดกินยา ระบบก็จะส่งข้อความเข้า LINE ของคุณทันทีครับ
